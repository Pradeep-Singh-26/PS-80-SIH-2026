# Shared dependencies for the full-solution build. Add to the relevant
# section only — do not reorganize this file without updating TEAM_SPLIT.md.

# Core
numpy
pandas
xarray
netCDF4
scipy
pyyaml
python-dotenv

# Track A (Pradeep) — ingestion, preprocessing, regime classifier
cdsapi              # ERA5 access
geopandas
rasterio
regionmask
xgboost
lightgbm
shap                # classifier explainability

# Track B (Baljeet) — correction, probability/uncertainty, MLOps
scikit-learn
xgboost
lightgbm
shap                # correction explainability
statsmodels         # quantile regression
mlflow              # model registry / experiment tracking

# Track C (Divyansh) — aggregation, verification, API, dashboard, ops
matplotlib
folium
fastapi
uvicorn
streamlit
jinja2
pytest
